comments:
  allowedFields: ["name", "comment", "replying_to"]
  branch: "gh-pages"
  commitMessage: "New Comment"
  filename: "c{@timestamp}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
  moderation: true
  path: "_data/c/{options.slug}"
  requiredFields: ["name", "comment"]
